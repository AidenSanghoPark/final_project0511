<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="dsn.mypage">
  	<update id="userUpdate" parameterType="Map">
  		update f_user set
  			u_nick=#{u_nick},
  			u_tel=#{u_tel},
  			u_email=#{u_email}
  			where u_idx=16
  		</update>
  	<delete id="pwdUpdate" parameterType="Map">
  		UPDATE F_USER SET
  			U_PWD=#{pwdconfirm}
  			WHERE u_idx=16
  	</delete>
  	<select id="pwdFind" resultType="dsn.mypage.model.MyPageDTO">
  	 select u_pwd from f_user where u_idx=16
  	</select>
  	<select id="totalCnt" resultType="Integer">
  	select count(*) from f_client
  	</select>
  	<select id="myPageList" parameterType="Map" resultType="dsn.contest.model.ConDTO">
  		select * from
			(select rownum as rnum,a.* from
			(select * from f_client order by c_idx desc)a)b
			where rnum<![CDATA[>=]]>#{start} and rnum<![CDATA[<=]]>#{end}
  	</select>
  </mapper>